<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Main features -->
    <group android:id="@+id/main_features">
        <item
            android:id="@+id/transactionsFragment"
            android:icon="@android:drawable/ic_menu_agenda"
            android:title="All Transactions"/>
    </group>

    <!-- Extra items not in bottom navigation -->
    <group android:id="@+id/extra_features">
        <item
            android:id="@+id/categoriesFragment"
            android:icon="@android:drawable/ic_menu_view"
            android:title="Categories"/>

        <item
            android:id="@+id/merchantsFragment"
            android:icon="@android:drawable/ic_menu_manage"
            android:title="Merchants"/>
    </group>

    <group android:id="@+id/system">
        <item
            android:id="@+id/syncFragment"
            android:icon="@android:drawable/ic_menu_rotate"
            android:title="Sync"/>

        <item
            android:id="@+id/settingsFragment"
            android:icon="@android:drawable/ic_menu_preferences"
            android:title="Settings"/>
    </group>

</menu>


